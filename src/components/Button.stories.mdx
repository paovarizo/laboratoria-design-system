import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { DefaultTemplate } from "./Button.templates.js";
import Button from "./Button";

export const VISIBLE_CONTROLS = ["color"];

<Meta
  title="Components/Button"
  component={Button}
  args={{
    color: "green",
  }}
  parameters={{
    controls: { include: VISIBLE_CONTROLS },
  }}
  argTypes={{
    color: {
      control: { type: "select" },
      options: ["gray", "blue", "green"],
    },
  }}
/>

<Canvas>
  <Story name="Color">{DefaultTemplate.bind({})}</Story>
</Canvas>
