import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { DefaultTemplate } from "./Button.templates.js";
import Button from "./Button";

export const VISIBLE_CONTROLS = ["variant"];
export const VISIBLE_PROPS = ["variant"];

<Meta
  title="Components/Button"
  component={Button}
  args={{
    variant: "register",
  }}
  parameters={{
    controls: { include: VISIBLE_CONTROLS },
  }}
  argTypes={{
    variant: {
      control: { type: "select" },
      options: ["register", "login"],
    },
  }}
/>

# Button

Weave buttons trigger actions or changes within the interface. They come in a number of varieties.

<Canvas>
  <Story name="Color">{DefaultTemplate.bind({})}</Story>
</Canvas>
